package other;

/**
 * 返回在[0, X)区间上的数出现的概率位x^k; (0 < x <= 1)
 * @author LEOSNOW
 */
public class PossibleProblem {
	public static double getNum() {
		return Math.max(Math.random(), Math.random());
	}
	

}
